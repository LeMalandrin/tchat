import { ExistingLoginDirective } from './existing-login.directive';

describe('ExistingLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingLoginDirective();
    expect(directive).toBeTruthy();
  });
});
