import { AlreadyConnectedDirective } from './already-connected.directive';

describe('AlreadyConnectedDirective', () => {
  it('should create an instance', () => {
    const directive = new AlreadyConnectedDirective();
    expect(directive).toBeTruthy();
  });
});
